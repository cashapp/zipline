allprojects {
  buildscript {
    repositories {
      jcenter()
      google()
    }
  }
  repositories {
    jcenter()
    google()
  }
}

ext {
  compileSdkVersion = 26
  buildToolsVersion = '26.0.2'
}
